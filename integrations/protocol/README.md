# Tiny Cosmos Protocol Fixtures

These fixtures are harness-neutral examples for future TypeScript adapters. They
exercise manager protocol v1 envelopes and guest-control payloads without
including Pi or OpenCode adapter code.

Adapter compatibility tests should deserialize these files, preserve exact
operation names, and treat all IDs as opaque strings.
